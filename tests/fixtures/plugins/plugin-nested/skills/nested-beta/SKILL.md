---
name: nested-beta
description: Second nested skill — name already starts with the plugin namespace prefix.
---

Nested-beta skill body. This tests the namespace-dedup behavior (skill name already starts with "nested-", so the installer should NOT prepend a second "nested-" prefix).
